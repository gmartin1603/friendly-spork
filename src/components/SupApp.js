import React from 'react';

function SupApp(props) {
    return (
        <div>
            <Header name={profile.name} role={profile.role} tabs={['Home', 'Postings', 'Edit Profile']} />
            <h1>Sup App View</h1>
        </div>
    );
}

export default SupApp;