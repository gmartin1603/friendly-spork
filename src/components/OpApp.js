import React from 'react';
import Header from './Header';

function OpApp(props) {
    return (
        <div>
            <Header/>
            <h1>Op App View</h1>
        </div>
    );
}

export default OpApp;